# Atlas backend base URL — required for API-connected screens
# Leave empty to use the Vite dev proxy (recommended for local development)
# Authentication is handled via POST /auth/login — no env vars required for auth
VITE_API_BASE_URL=
