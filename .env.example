# Atlas backend base URL — required for CadastralMovimentDefaut (API-connected screen)
# Backend HTTP port (redirects to HTTPS 7113 — leave empty when using Vite proxy)
VITE_API_BASE_URL=
VITE_CLIENT_ID=dev-client
VITE_CLIENT_SECRET=dev-secret-123
